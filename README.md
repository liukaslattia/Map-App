Uses simple RESTful API for testing purposes.
The API contains location information (latitude, longitude) and can be used via HTTP commands
RESTful API source: https://github.com/pohjus/restful-api-nodejs
